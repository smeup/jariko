     H ACTGRP('CUSTOM')
      *
      * Call a pgm with the same ACTGRP (CUSTOM)
     C                   CALL      'ACTGRP_04'
      *
      * Call a pgm with the same ACTGRP (CUSTOM)
     C                   CALL      'ACTGRP_04'
      *
      * Call a pgm with the same ACTGRP (CUSTOM)
     C                   CALL      'ACTGRP_04'
      *
     C                   SETON                                          LR